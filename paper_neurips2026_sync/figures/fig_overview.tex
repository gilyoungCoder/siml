\documentclass[tikz,border=6pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,backgrounds,fit,calc,decorations.pathmorphing,shadows}

% ---- palette ----
\definecolor{packBG}{HTML}{DBEAFE}
\definecolor{packAccent}{HTML}{2563EB}
\definecolor{casRed}{HTML}{DC2626}
\definecolor{txtBG}{HTML}{DCFCE7}
\definecolor{txtAccent}{HTML}{059669}
\definecolor{imgBG}{HTML}{FED7AA}
\definecolor{imgAccent}{HTML}{EA580C}
\definecolor{howPurple}{HTML}{7C3AED}
\definecolor{anchorBG}{HTML}{E0E7FF}
\definecolor{hybridBG}{HTML}{EDE9FE}
\definecolor{railA}{HTML}{FCA5A5}
\definecolor{railB}{HTML}{FCD34D}
\definecolor{railC}{HTML}{86EFAC}
\definecolor{railD}{HTML}{93C5FD}
\definecolor{greyLine}{HTML}{4B5563}

\begin{document}
\begin{tikzpicture}[
    font=\footnotesize,
    >={Stealth[length=2.2mm,width=1.8mm]},
    thin,
    node distance=3mm,
    arrw/.style={->, line width=0.8pt, draw=greyLine},
    bigarrow/.style={->, line width=1.6pt, draw=greyLine},
    thumb/.style={draw=packAccent!70,line width=0.4pt,fill=white,minimum width=5.5mm,minimum height=4mm,inner sep=0pt},
    chip/.style={draw=packAccent,line width=0.5pt,fill=white,rounded corners=1pt,inner sep=2pt,font=\scriptsize},
    tag/.style={draw=black!40,fill=black!3,rounded corners=1pt,inner sep=2pt,font=\scriptsize},
    stageTitle/.style={font=\small\bfseries},
    colBG/.style={rounded corners=3pt,draw=black!20,line width=0.4pt},
]

% ============== LAYOUT CONSTANTS ==============
% total width ~ 24cm, height ~10cm, aspect ~2.4:1
\def\colW{5.4}     % column width
\def\colWinner{5.0}% inner (colW - 0.4)
\def\gap{0.9}      % gap between columns
\def\figH{10.0}    % figure height

% column x-centers
\pgfmathsetmacro{\Ax}{0}
\pgfmathsetmacro{\Bx}{\Ax + \colW + \gap}
\pgfmathsetmacro{\Cx}{\Bx + \colW + \gap}
\pgfmathsetmacro{\Dx}{\Cx + \colW + \gap}

% ============== TOP BANNER (across B-D) ==============
\coordinate (bannerL) at ($(\Bx - \colW/2, 4.7)$);
\coordinate (bannerR) at ($(\Dx + \colW/2, 4.7)$);
\pgfmathsetmacro{\bannerW}{\Dx + \colW/2 - (\Bx - \colW/2)}
\node[fill=black!5,draw=black!20,rounded corners=2pt,inner sep=2pt,
      minimum width=\bannerW cm,
      minimum height=5mm,
      anchor=center,font=\scriptsize\itshape]
      (banner) at ($(bannerL)!0.5!(bannerR)$) {F parallel family lanes};

% four faint rails beneath banner spanning B-D
\foreach \i/\c in {0/railA,1/railB,2/railC,3/railD} {
  \pgfmathsetmacro{\yr}{4.35 - 0.15*\i}
  \draw[\c,line width=1.2pt,opacity=0.55]
    (\Bx - \colW/2, \yr) -- (\Dx + \colW/2, \yr);
}

% ============== COLUMN A : CONCEPT PACK ==============
% background
\node[colBG,fill=packBG,minimum width=\colW cm,minimum height=9.6cm,anchor=center]
     (colA) at (\Ax,0) {};
\node[stageTitle,anchor=north,text=packAccent] at (\Ax,4.55) {A. Concept Pack};
\node[font=\scriptsize\itshape,anchor=north,text=packAccent!80!black] at (\Ax,4.15) {user-editable, training-free};

% 4x4 thumbnail grid
\def\gridX{\Ax - 1.25}
\def\gridY{3.0}
\node[anchor=south west,font=\scriptsize] at (\gridX+0.1,\gridY+0.15) {\footnotesize K=4 exemplars};
\foreach \r/\lab in {0/shocking,1/illegal,2/violence,3/nudity} {
  \node[anchor=east,font=\scriptsize] at (\gridX - 0.05, \gridY - 0.42 - 0.55*\r) {\lab};
  \foreach \c in {0,1,2,3} {
    \pgfmathsetmacro{\tx}{\gridX + 0.05 + 0.6*\c}
    \pgfmathsetmacro{\ty}{\gridY - 0.15 - 0.55*\r}
    \node[thumb,anchor=north west] at (\tx,\ty) {};
  }
}
% grid frame
\draw[packAccent!60,line width=0.4pt,rounded corners=1pt]
  (\gridX, \gridY - 2.35) rectangle (\gridX + 2.5, \gridY - 0.15);

% two stacked vector chips
\node[chip,minimum width=3.6cm,anchor=center] (dtgt) at (\Ax, -0.3)
   {$\bar d^{\,tgt}_f$  \ (per-family target)};
\node[chip,minimum width=3.6cm,anchor=center,below=1mm of dtgt] (danc)
   {$\bar d^{\,anc}_f$  \ (per-family anchor)};

% keywords chip
\node[chip,minimum width=3.6cm,anchor=center,below=2mm of danc] (kw)
   {keywords $\mathcal{W}_f$};

% exit arrow right (to column B)
\coordinate (Aexit) at (\Ax + \colW/2, -0.8);
\draw[bigarrow] (Aexit) -- ++(\gap,0);
\node[font=\scriptsize,anchor=south,align=center]
  at ($(Aexit)+(\gap/2,0.05)$) {per-family\\dirs \& kw.};

% ============== COLUMN B : DENOISING LOOP ==============
\node[colBG,dashed,line width=0.5pt,fill=white,
      minimum width=\colW cm,minimum height=8.4cm,anchor=center]
     (colB) at (\Bx,-0.6) {};
\node[stageTitle,anchor=north] at (\Bx,3.25) {B. Denoising loop};

% self-loop: z_t -> eps_theta -> z_{t-1}
\node[draw,rounded corners=2pt,fill=white,minimum width=1.0cm,minimum height=0.7cm]
     (zt) at (\Bx - 1.6, 2.1) {$z_t$};
\node[draw,rounded corners=2pt,fill=black!5,minimum width=1.4cm,minimum height=0.7cm,right=5mm of zt]
     (eps) {$\epsilon_\theta$};
\node[draw,rounded corners=2pt,fill=white,minimum width=1.2cm,minimum height=0.7cm,right=5mm of eps]
     (ztm) {$z_{t-1}$};
\draw[arrw] (zt) -- (eps);
\draw[arrw] (eps) -- (ztm);
% loop back
\draw[arrw] (ztm.north) .. controls +(0,0.7) and +(0,0.7) .. (zt.north);
\node[font=\scriptsize,anchor=south] at ($(zt)!0.5!(ztm)+(0,0.55)$) {loop};

% CAS diamond (red)
\node[diamond,draw=casRed,line width=0.8pt,fill=casRed!8,aspect=2.4,
      inner sep=2pt,minimum width=4.6cm,minimum height=1.9cm,
      align=center]
     (cas) at (\Bx, 0.3)
     {\scriptsize\textbf{CAS gate}\\[1pt]
      $\displaystyle \mathrm{CAS}(t)=\max_f \cos(d_{prompt},\bar d^{\,tgt}_f) > \tau$};
\node[font=\scriptsize,anchor=north,text=casRed!80!black] at (\Bx, -0.85)
     {$\tau\in[0.4,0.6]$, sticky after first fire};

% epsilon feeds into CAS
\draw[arrw] (eps.south) .. controls +(0,-0.6) and +(0,0.7) .. (cas.north);

% fire exit
\node[font=\scriptsize,text=txtAccent!60!black] at (\Bx + 1.1, -1.5)
     {\checkmark\ fire};
\coordinate (Bexit) at (\Bx + \colW/2, -1.7);
\draw[bigarrow] (cas.east) .. controls +(0.6,0) and +(-0.4,0.2) .. (Bexit);

% incoming arrow from A already drawn; connect to the loop input side
\draw[arrw] (\Ax + \colW/2 + \gap, -0.8) -- (\Bx - \colW/2 + 0.2, -0.8)
  -- (\Bx - \colW/2 + 0.2, 2.1) -- (zt.west);

% ============== COLUMN C : DUAL PROBE ==============
\node[colBG,fill=white,minimum width=\colW cm,minimum height=8.4cm,anchor=center]
     (colC) at (\Cx,-0.6) {};
\node[stageTitle,anchor=north] at (\Cx,3.25) {C. Where: dual probe};

% --- TOP sub-row: Text probe ---
\node[colBG,fill=txtBG,minimum width=\colWinner cm,minimum height=3.1cm,anchor=north]
     (txtBox) at (\Cx, 2.75) {};
\node[anchor=north,font=\scriptsize\bfseries,text=txtAccent] at (\Cx,2.65) {Text probe};

% stylized 4x4 cross-attention heatmap (one column red)
\def\tmX{\Cx - 1.9}
\def\tmY{2.15}
\foreach \r in {0,1,2,3} {
  \foreach \c in {0,1,2,3} {
    \pgfmathsetmacro{\tx}{\tmX + 0.28*\c}
    \pgfmathsetmacro{\ty}{\tmY - 0.28*\r}
    \pgfmathtruncatemacro{\isRed}{(\c==2)?1:0}
    \ifnum\isRed=1
      \node[draw=casRed!60,fill=casRed!55,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
    \else
      \pgfmathsetmacro{\opa}{0.15 + 0.15*mod(\r+\c,3)}
      \node[draw=txtAccent!40,fill=txtAccent!30,fill opacity=\opa,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
    \fi
  }
}
% formula beneath
\node[anchor=north,font=\scriptsize] at (\Cx + 0.35, 2.1)
  {$M^f_{\text{text}} = \sigma\!\big(\alpha(\max\limits_{w\in\mathcal{W}_f} A_w - \theta_{\text{text}})\big)$};

% --- BOTTOM sub-row: Image probe ---
\node[colBG,fill=imgBG,minimum width=\colWinner cm,minimum height=3.1cm,anchor=north]
     (imgBox) at (\Cx, -0.55) {};
\node[anchor=north,font=\scriptsize\bfseries,text=imgAccent] at (\Cx,-0.65) {Image probe};

% CLIP-feature 4x4 grid with scattered orange
\def\imX{\Cx - 1.9}
\def\imY{-1.15}
\foreach \r/\act in {0/{1,3},1/{0},2/{2,3},3/{1}} {
  \foreach \c in {0,1,2,3} {
    \pgfmathsetmacro{\tx}{\imX + 0.28*\c}
    \pgfmathsetmacro{\ty}{\imY - 0.28*\r}
    \node[draw=imgAccent!40,fill=imgAccent!15,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
  }
}
\foreach \r/\cc in {0/1,0/3,1/0,2/2,2/3,3/1} {
  \pgfmathsetmacro{\tx}{\imX + 0.28*\cc}
  \pgfmathsetmacro{\ty}{\imY - 0.28*\r}
  \node[draw=imgAccent,fill=imgAccent!70,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
}
\node[anchor=north,font=\scriptsize] at (\Cx + 0.3, -1.2)
  {$M^f_{\text{img}} = \sigma\!\big(\alpha(\cos(f,\bar e_f) - \theta_{\text{img}})\big)$};

% max_f converging funnel (right side, between both sub-rows)
\coordinate (funTopL) at (\Cx + \colW/2 - 0.6, 1.3);
\coordinate (funTopR) at (\Cx + \colW/2 - 0.1, 0.55);
\coordinate (funBotL) at (\Cx + \colW/2 - 0.6, -2.2);
\coordinate (funBotR) at (\Cx + \colW/2 - 0.1, -1.45);
\draw[draw=black!50,fill=black!6,line width=0.4pt]
  (funTopL) -- (funTopR) -- (funBotR) -- (funBotL) -- cycle;
\node[font=\scriptsize] at (\Cx + \colW/2 - 0.35, -0.45) {$\max_f$};

% fusion box
\node[draw,rounded corners=2pt,fill=black!4,line width=0.4pt,
      minimum width=3.6cm,minimum height=0.7cm,anchor=center,font=\scriptsize]
     (fuse) at (\Cx, -2.7)
     {$M = \min\!\big(1,\; M_{\text{text}} + M_{\text{img}}\big)$};

% connect text/img to funnel, funnel to fuse
\draw[arrw] (\Cx + 1.1, 1.55) -- (funTopL |- 0,0.9);
\draw[arrw] (\Cx + 1.1, -1.75) -- (funBotL |- 0,-1.75);
\draw[arrw] (\Cx + \colW/2 - 0.1, -0.45) -- ++(0.3,0) |- (fuse.east);

% mask thumbnail (gradient square)
\node[draw=black!40,line width=0.3pt,
      minimum width=6mm,minimum height=6mm,
      inner sep=0pt,left=2mm of fuse,
      fill=black!20,anchor=east] (maskThumb) at (\Cx - 2.0, -2.7) {};
\shade[left color=black!5,right color=casRed!60,rounded corners=0.5pt]
  ($(maskThumb.south west)$) rectangle ($(maskThumb.north east)$);
\draw[black!40,line width=0.3pt] ($(maskThumb.south west)$) rectangle ($(maskThumb.north east)$);
\node[font=\scriptsize,anchor=east] at ($(maskThumb.west)+(-0.05,0)$) {$M$};

% incoming arrow from B to C (enters top-left area)
\draw[bigarrow] (\Bx + \colW/2, -1.7) -- (\Cx - \colW/2, -1.7);

% exit arrow to D (mask goes to How)
\coordinate (Cexit) at (\Cx + \colW/2, -2.7);
\draw[bigarrow] (fuse.east) -- (Cexit) -- ++(\gap,0);

% ============== COLUMN D : HOW (two modes) ==============
\node[colBG,fill=white,minimum width=\colW cm,minimum height=8.4cm,anchor=center]
     (colD) at (\Dx,-0.6) {};
\node[stageTitle,anchor=north,text=howPurple] at (\Dx,3.25) {D. How: two modes};

% OR-switch diamond (central)
\node[diamond,draw=howPurple,fill=howPurple!8,line width=0.6pt,aspect=1.6,
      inner sep=1pt,minimum width=1.4cm,minimum height=0.9cm,font=\scriptsize]
     (orsw) at (\Dx - 1.7, 0.3) {OR};

% Anchor-inpaint branch (top)
\node[colBG,fill=anchorBG,minimum width=\colWinner cm,minimum height=2.6cm,anchor=north]
     (anchorBox) at (\Dx + 0.3, 2.6) {};
\node[anchor=north,font=\scriptsize\bfseries,text=packAccent] at (\Dx + 0.3,2.5)
     {Anchor inpaint};
\node[anchor=center,font=\scriptsize] at (\Dx + 0.3, 1.75)
     {$\hat{\epsilon}_{\text{safe}} = \hat{\epsilon}_t - s_a \cdot M \cdot \tfrac{1}{F}\sum\limits_f d^{\,tgt}_f$};
\node[tag,anchor=center] at (\Dx + 0.3, 1.0)
     {$s_a\in[0.5,3]$, \ 1$\times$ compute};

% Hybrid branch (bottom)
\node[colBG,fill=hybridBG,minimum width=\colWinner cm,minimum height=2.6cm,anchor=north]
     (hybBox) at (\Dx + 0.3, -0.5) {};
\node[anchor=north,font=\scriptsize\bfseries,text=howPurple] at (\Dx + 0.3,-0.6)
     {Hybrid};
\node[anchor=center,font=\scriptsize] at (\Dx + 0.3, -1.35)
     {$\hat{\epsilon}_{\text{safe}} = \hat{\epsilon}_t + s_h \cdot M \cdot \tfrac{1}{F}\sum\limits_f (d^{\,anc}_f - d^{\,tgt}_f)$};
\node[tag,anchor=center] at (\Dx + 0.3, -2.05)
     {$s_h\in[10,20]$, \ +1 anchor forward};

% connect OR-switch to both branches
\draw[arrw] (orsw.north) .. controls +(0,0.4) and +(-0.4,0) .. (anchorBox.west);
\draw[arrw] (orsw.south) .. controls +(0,-0.4) and +(-0.4,0) .. (hybBox.west);

% incoming from C
\draw[arrw] (\Cx + \colW/2 + \gap - 0.1, -2.7) -- (\Dx - \colW/2 + 0.1, -2.7)
   -- (\Dx - \colW/2 + 0.1, 0.3) -- (orsw.west);

% merge arrow from both branches to single "safe eps"
\coordinate (mergeIn1) at (\Dx + \colW/2 - 0.6, 1.75);
\coordinate (mergeIn2) at (\Dx + \colW/2 - 0.6, -1.35);
\coordinate (mergeNode) at (\Dx + \colW/2 - 0.2, 0.2);
\draw[arrw] (anchorBox.east) -- ++(0.15,0) |- (mergeNode);
\draw[arrw] (hybBox.east)    -- ++(0.15,0) |- (mergeNode);

\node[draw,circle,fill=black!5,inner sep=1pt,minimum size=4mm,font=\scriptsize]
     (safeEps) at (mergeNode) {};
\node[anchor=west,font=\scriptsize] at ($(safeEps.east)+(0.05,0)$) {safe $\hat{\epsilon}$};

% feedback to B loop: route WAY above the top banner to avoid title overlap
\coordinate (fbTop) at (\Dx, 5.7);
\coordinate (fbTopL) at (\Bx, 5.7);
\draw[bigarrow,draw=howPurple!70!black,dashed]
  (safeEps.north) -- ++(0,0.6) -| (\Dx + \colW/2 - 0.35, 5.7)
  -- (\Bx - \colW/2 + 0.5, 5.7) -- (\Bx - \colW/2 + 0.5, 2.1) -- (zt.west);
\node[font=\scriptsize,text=howPurple!70!black,anchor=south]
  at (\Cx, 5.8) {safe $\hat{\epsilon}$ $\rightarrow$ loop};

% terminal exit to safe image thumbnail
\node[draw=black!50,line width=0.4pt,fill=black!5,
      minimum width=1.1cm,minimum height=0.9cm,rounded corners=1pt,
      right=6mm of safeEps,anchor=west,font=\scriptsize,align=center]
     (safeImg) {safe\\image};
\draw[bigarrow] (safeEps.east) -- (safeImg.west);

% ============== COLUMN A OUTGOING ARROW FIX ==============
% (re-label cleaned)

\end{tikzpicture}
\end{document}
