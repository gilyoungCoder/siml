\documentclass[tikz,border=6pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,backgrounds,fit,calc,decorations.pathmorphing,decorations.pathreplacing,shadows}

% ---- palette ----
\definecolor{packBG}{HTML}{DBEAFE}
\definecolor{packAccent}{HTML}{2563EB}
\definecolor{casRed}{HTML}{DC2626}
\definecolor{txtBG}{HTML}{DCFCE7}
\definecolor{txtAccent}{HTML}{059669}
\definecolor{imgBG}{HTML}{FED7AA}
\definecolor{imgAccent}{HTML}{EA580C}
\definecolor{howPurple}{HTML}{7C3AED}
\definecolor{anchorBG}{HTML}{E0E7FF}
\definecolor{hybridBG}{HTML}{EDE9FE}
\definecolor{railA}{HTML}{FCA5A5}
\definecolor{railB}{HTML}{FCD34D}
\definecolor{railC}{HTML}{86EFAC}
\definecolor{railD}{HTML}{93C5FD}
\definecolor{greyLine}{HTML}{4B5563}

\begin{document}
\begin{tikzpicture}[
    font=\footnotesize,
    >={Stealth[length=2.6mm,width=2.2mm]},
    thin,
    arrw/.style={->, line width=1.1pt, draw=greyLine},
    bigarrow/.style={->, line width=1.7pt, draw=greyLine},
    busline/.style={line width=1.1pt, draw=greyLine},
    thumb/.style={draw=packAccent!70,line width=0.4pt,fill=white,minimum width=5.5mm,minimum height=4mm,inner sep=0pt},
    chip/.style={draw=packAccent,line width=0.5pt,fill=white,rounded corners=1pt,inner sep=2.5pt,font=\scriptsize},
    stageTitle/.style={font=\small\bfseries},
    colBG/.style={rounded corners=3pt,draw=black!20,line width=0.4pt},
]

% ============== LAYOUT CONSTANTS ==============
\def\colW{5.6}     % column width for B/C/D
\def\colWA{6.4}    % WIDER width for column A (concept-pack labels + brace)
\def\colWin{4.0}   % inner box width  (reduced so max_f funnel has room)
\def\colDin{5.0}   % inner box width for D column (tighter than col so boxes
                   %  visually "float" with side margin; Hybrid equation fits)
\def\gap{1.25}     % gap between columns for breathing room

\pgfmathsetmacro{\Ax}{0}
% A uses \colWA (wider); B/C/D use \colW — edge-to-edge gap is \gap
\pgfmathsetmacro{\Bx}{\Ax + \colWA/2 + \colW/2 + \gap}
\pgfmathsetmacro{\Cx}{\Bx + \colW + \gap}
\pgfmathsetmacro{\Dx}{\Cx + \colW + \gap}

% ============== TOP BANNER (across B-D) ==============
\pgfmathsetmacro{\bannerW}{\Dx + \colW/2 - (\Bx - \colW/2)}
\node[fill=black!5,draw=black!20,rounded corners=2pt,inner sep=2pt,
      minimum width=\bannerW cm,
      minimum height=5mm,
      anchor=center,font=\scriptsize\itshape]
      (banner) at ($(\Bx - \colW/2, 4.85)!0.5!(\Dx + \colW/2, 4.85)$) {F parallel family lanes};

\foreach \i/\c in {0/railA,1/railB,2/railC,3/railD} {
  \pgfmathsetmacro{\yr}{4.52 - 0.16*\i}
  \draw[\c,line width=1.2pt,opacity=0.55]
    (\Bx - \colW/2, \yr) -- (\Dx + \colW/2, \yr);
}

% ============== COLUMN A : CONCEPT PACK ==============
\node[colBG,fill=packBG,minimum width=\colWA cm,minimum height=9.6cm,anchor=center]
     (colA) at (\Ax,0) {};
\node[stageTitle,anchor=north,text=packAccent] at (\Ax,4.6) {A.~Concept Pack};
\node[font=\scriptsize\itshape,anchor=north,text=packAccent!80!black] at (\Ax,4.15)
     {user-editable, training-free};

% header: emphasize the two axes of the pack — F concepts/families × K exemplars
\node[font=\scriptsize,anchor=north] at (\Ax, 3.7)
     {$F$ families $\times$ $K$ exemplars per family};

\def\gridX{\Ax - 1.25}
\def\gridY{3.25}
\foreach \r/\lab in {0/shocking,1/illegal,2/violence,3/nudity} {
  % row label = "family name" with a leading f_i chip so the per-concept
  % family structure is visually unambiguous
  \node[anchor=east,font=\scriptsize] at (\gridX - 0.1, \gridY - 0.42 - 0.55*\r)
       {$f_{\the\numexpr\r+1}$: \lab};
  \foreach \c in {0,1,2,3} {
    \pgfmathsetmacro{\tx}{\gridX + 0.05 + 0.6*\c}
    \pgfmathsetmacro{\ty}{\gridY - 0.15 - 0.55*\r}
    \node[thumb,anchor=north west] at (\tx,\ty) {};
  }
}
\draw[packAccent!60,line width=0.4pt,rounded corners=1pt]
  (\gridX, \gridY - 2.35) rectangle (\gridX + 2.5, \gridY - 0.15);

% RIGHT brace spanning all rows → visually marks the per-concept family axis
\draw[decorate,decoration={brace,amplitude=4pt},
      line width=0.5pt,draw=packAccent!70]
  (\gridX + 2.58, \gridY - 0.15) -- (\gridX + 2.58, \gridY - 2.35);
\node[font=\scriptsize\itshape,text=packAccent!85!black,anchor=west]
  at (\gridX + 2.72, \gridY - 1.25) {$F$ families};

\node[chip,minimum width=4.2cm,anchor=center] (dtgt) at (\Ax, -0.2)
   {$\bar d^{\,tgt}_f$ \ (per-family target)};
\node[chip,minimum width=4.2cm,anchor=center,below=1.5mm of dtgt] (danc)
   {$\bar d^{\,anc}_f$ \ (per-family anchor)};
\node[chip,minimum width=4.2cm,anchor=center,below=2.5mm of danc] (kw)
   {keywords $\mathcal{W}_f$};

\coordinate (Aexit) at (\Ax + \colWA/2, -1.3);

% ============== COLUMN B : DENOISING LOOP ==============
\node[colBG,dashed,line width=0.5pt,fill=white,
      minimum width=\colW cm,minimum height=9.6cm,anchor=center]
     (colB) at (\Bx,-0.95) {};
\node[stageTitle,anchor=north] at (\Bx,3.25) {B.~Denoising loop};

\node[draw,rounded corners=2pt,fill=white,minimum width=1.0cm,minimum height=0.7cm]
     (zt) at (\Bx - 1.7, 2.0) {$z_t$};
\node[draw,rounded corners=2pt,fill=black!5,minimum width=1.4cm,minimum height=0.7cm,right=5mm of zt]
     (eps) {$\epsilon_\theta$};
\node[draw,rounded corners=2pt,fill=white,minimum width=1.2cm,minimum height=0.7cm,right=5mm of eps]
     (ztm) {$z_{t-1}$};
\draw[arrw] (zt) -- (eps);
\draw[arrw] (eps) -- (ztm);
\draw[arrw] (ztm.north) .. controls +(0,0.45) and +(0,0.45) .. (zt.north);

% CAS gate — switched from diamond to ROUNDED RECTANGLE so it fits cleanly
% inside column B (no corners poking across the dashed B boundary).
\node[draw=casRed,line width=0.8pt,fill=casRed!8,rounded corners=3pt,
      inner sep=3pt,minimum width=4.2cm,minimum height=1.35cm,
      align=center]
     (cas) at (\Bx, -1.1)
     {\scriptsize\textbf{CAS gate}\\[1pt]
      $\displaystyle \mathrm{CAS}(t)=\max_f \cos(d_{prompt},\bar d^{\,tgt}_f)\,>\,\tau$};

% straight vertical feeder from eps to CAS
\draw[arrw] (eps.south) -- (cas.north);

% CAS output → C : bulk of the arrow is in the B-C GAP; only the very
% start (inside B to reach cas.east) and the head (at C's west wall)
% touch the column walls.
\draw[bigarrow] (cas.east) -- (\Cx - \colW/2, -1.55);
\node[font=\scriptsize,text=txtAccent!60!black,anchor=west]
     at ($(cas.east)+(0.10,0.25)$) {\checkmark\ fire};

% A -> B : unified arrow. Bulk of the line sits in the A-B GAP (at
% x = Bx - colW/2 - 0.1, i.e. 0.1 cm OUTSIDE B's west wall) so the
% arrow does not run through the interior of B; only the final
% horizontal stub crosses into B to reach zt.west.
\draw[bigarrow] (Aexit) -- (\Bx - \colW/2 - 0.1, -1.3)
  -- (\Bx - \colW/2 - 0.1, 2.0) -- (zt.west);

% ============== COLUMN C : DUAL PROBE ==============
\node[colBG,fill=white,minimum width=\colW cm,minimum height=9.6cm,anchor=center]
     (colC) at (\Cx,-0.95) {};
\node[stageTitle,anchor=north] at (\Cx,3.25) {C.~Where: dual probe};

% The probe boxes are now NARROWER (colWin=4.0) — shifted LEFT within the column
% so the max_f funnel can sit on the right half cleanly without overlap.
\pgfmathsetmacro{\Ccx}{\Cx - 0.55}   % probe-box center (shifted left from column center)

% --- TOP sub-row: Text probe ---
\node[colBG,fill=txtBG,minimum width=\colWin cm,minimum height=3.1cm,anchor=north]
     (txtBox) at (\Ccx, 2.75) {};
% header pushed down slightly for breathing room from box top edge
\node[anchor=north,font=\scriptsize\bfseries,text=txtAccent] at (\Ccx,2.55) {Text probe};

% matrix centered within box, red cells form a 2x2 COMPACT CLUSTER
% (represents spatially LOCALIZED keyword attention — not a uniform token stripe)
\def\tmX{\Ccx - 0.55}
\def\tmY{2.00}
\foreach \r in {0,1,2,3} {
  \foreach \c in {0,1,2,3} {
    \pgfmathsetmacro{\tx}{\tmX + 0.30*\c}
    \pgfmathsetmacro{\ty}{\tmY - 0.26*\r}
    \pgfmathsetmacro{\opa}{0.15 + 0.15*mod(\r+\c,3)}
    \node[draw=txtAccent!40,fill=txtAccent!30,fill opacity=\opa,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
  }
}
\foreach \r/\c in {1/1,1/2,2/1,2/2} {
  \pgfmathsetmacro{\tx}{\tmX + 0.30*\c}
  \pgfmathsetmacro{\ty}{\tmY - 0.26*\r}
  \node[draw=casRed!60,fill=casRed!55,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
}
% formula: max over keyword attentions
\node[anchor=north,font=\scriptsize] at (\Ccx, 0.55)
  {$M^f_{\mathrm{text}} \propto \sigma\!\big(\textstyle\max_{w\in\mathcal{W}_f}\! A_w\big)$};

% --- BOTTOM sub-row: Image probe ---
\node[colBG,fill=imgBG,minimum width=\colWin cm,minimum height=3.1cm,anchor=north]
     (imgBox) at (\Ccx, -0.55) {};
% header pushed down slightly
\node[anchor=north,font=\scriptsize\bfseries,text=imgAccent] at (\Ccx,-0.75) {Image probe};

% matrix centered within box
\def\imX{\Ccx - 0.55}
\def\imY{-1.30}
\foreach \r in {0,1,2,3} {
  \foreach \c in {0,1,2,3} {
    \pgfmathsetmacro{\tx}{\imX + 0.30*\c}
    \pgfmathsetmacro{\ty}{\imY - 0.26*\r}
    \node[draw=imgAccent!40,fill=imgAccent!15,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
  }
}
% scattered activations — spatially distributed (more spread than text's compact cluster)
\foreach \r/\c in {0/1,0/3,1/0,2/2,2/3,3/1} {
  \pgfmathsetmacro{\tx}{\imX + 0.30*\c}
  \pgfmathsetmacro{\ty}{\imY - 0.26*\r}
  \node[draw=imgAccent,fill=imgAccent!70,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
}
\node[anchor=north,font=\scriptsize] at (\Ccx, -2.65)
  {$M^f_{\mathrm{img}} \propto \sigma\!\big(\cos(f,\bar e_f)\big)$};

% max_f funnel — pushed FURTHER RIGHT (closer to column right edge) + 1.0cm wide
\pgfmathsetmacro{\funL}{\Cx + 1.80}   % left edge of funnel trapezoid
\pgfmathsetmacro{\funR}{\Cx + 2.75}   % right edge (column C ends at Cx+2.8)
\coordinate (funTopL) at (\funL, 1.4);
\coordinate (funTopR) at (\funR, 0.6);
\coordinate (funBotL) at (\funL, -2.4);
\coordinate (funBotR) at (\funR, -1.6);
\draw[draw=black!50,fill=black!6,line width=0.4pt]
  (funTopL) -- (funTopR) -- (funBotR) -- (funBotL) -- cycle;
% Funnel now represents MODALITY union (max of text/img) WITHIN a family,
% followed by WINNER-TAKE-ALL across families. Label shows both steps.
\node[font=\scriptsize,align=center] at ($(funTopL)!0.5!(funBotR)$)
     {\shortstack{$\max$\\[-1pt]\scriptsize mod}};

% fusion box — centered in column C. Uses direct notation (M tilde for
% winning mask value, w for winning family index) instead of an indicator
% function — these are exactly the two quantities consumed by the D column.
\node[draw,rounded corners=2pt,fill=black!4,line width=0.4pt,
      minimum width=4.8cm,minimum height=1.25cm,anchor=center,font=\scriptsize,
      align=center,inner sep=4pt]
     (fuse) at (\Cx, -4.70)
     {$M^f \,=\, \max\!\bigl(M^f_{\mathrm{text}},\; M^f_{\mathrm{img}}\bigr)$\\[3pt]
      $\widetilde{M} \,=\, \max_f M^f,\quad \mathsf{w} \,=\, \arg\max_f M^f$};

% connect text/img matrix centers to funnel left edge, then funnel right → fuse
\draw[arrw] (\Ccx + 0.8, 1.65) -- (funTopL);
\draw[arrw] (\Ccx + 0.8, -1.65) -- (funBotL);
% funnel right-mid drops DOWN along the east side of imgBox (still inside
% column C, past imgBox's east wall), then LEFT into fuse.east.
% Head preserved — arrow visibly terminates at the fusion box.
\draw[arrw] ($(funTopR)!0.5!(funBotR)$) |- (fuse.east);

% ============== COLUMN D : HOW (two modes) ==============
\node[colBG,fill=white,minimum width=\colW cm,minimum height=9.6cm,anchor=center]
     (colD) at (\Dx,-0.95) {};
\node[stageTitle,anchor=north,text=howPurple] at (\Dx,3.25) {D.~How: two modes};

% Anchor-inpaint branch (TOP) — LERP substitution using WINNING family
% w = argmax_f M^f (pixel-wise). Matches Eq. \ref{eq:anchor_inpaint} in method.tex.
\node[colBG,fill=anchorBG,minimum width=\colDin cm,minimum height=2.2cm,anchor=north]
     (anchorBox) at (\Dx, 2.35) {};
\node[anchor=north,font=\scriptsize\bfseries,text=packAccent] at (\Dx,2.23)
     {Anchor inpaint};
\node[anchor=center,font=\scriptsize,align=center] at (\Dx, 1.45)
     {$\hat{\epsilon}_{\mathrm{safe}} = (1{-}\beta)\,\hat{\epsilon}_t + \beta\,\big(\epsilon_\varnothing + \omega\, d^{\,anc}_{\mathsf{w}}\big)$\\[1pt]
      \scriptsize$\beta = \min(s_a\,\widetilde{M},\,1)$};

% Hybrid branch (BOTTOM) — target-to-anchor push using winning family w
\node[colBG,fill=hybridBG,minimum width=\colDin cm,minimum height=2.2cm,anchor=north]
     (hybBox) at (\Dx, -0.25) {};
\node[anchor=north,font=\scriptsize\bfseries,text=howPurple] at (\Dx,-0.37)
     {Hybrid};
\node[anchor=center,font=\scriptsize] at (\Dx, -1.15)
     {$\hat{\epsilon}_{\mathrm{safe}} = \hat{\epsilon}_t + s_h\,\widetilde{M}\,\big(d^{\,anc}_{\mathsf{w}} - d^{\,tgt}_{\mathsf{w}}\big)$};

% incoming arrow from C -> D : vertical bus sits JUST OUTSIDE D's west
% wall (in the C-D gap), so the line does NOT run through the interior
% of D. Only the short fork horizontals cross into D to reach the box
% west edges — where the arrow heads live.
\coordinate (Denter) at (\Dx - \colW/2 - 0.1, -4.70);
\coordinate (Dfork)  at (\Dx - \colW/2 - 0.1, 0.6);
\draw[arrw]    (fuse.east) -- (Denter);   % single arrow from fuse through gap
\draw[busline] (Denter) -- (Dfork);        % vertical bus outside D, no head
\draw[arrw] (Dfork) |- (anchorBox.west);
\draw[arrw] (Dfork) |- (hybBox.west);

% merge point — coordinate JUST OUTSIDE D's east wall (in the post-D
% margin) so the vertical feedback trunk is not inside D. The short
% horizontal stubs from each box east cross D's east wall to reach it.
\coordinate (mergeNode) at (\Dx + \colW/2 + 0.1, 0.1);
\draw[busline] (anchorBox.east) -- ++(0.4,0) -- (mergeNode);
\draw[busline] (hybBox.east)    -- ++(0.4,0) -- (mergeNode);

% feedback to B loop — originates at mergeNode (outside D east), rises
% along x=Dx+colW/2+0.1 (outside D), crosses the top above the banner,
% and drops straight down into zt.north. Entering zt from the NORTH
% avoids a double-arrowhead pileup at zt.west with the A→zt line.
\draw[bigarrow,draw=howPurple!70!black,dashed]
  (mergeNode) -- (\Dx + \colW/2 + 0.1, 5.75)
  -- (\Bx - 1.7, 5.75) -- (zt.north);
\node[font=\scriptsize,text=howPurple!70!black,anchor=south]
  at (\Cx, 5.85) {guided update $\rightarrow$ loop};

\end{tikzpicture}
\end{document}
