\documentclass[tikz,border=6pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,calc}

% palette
\definecolor{explBG}{HTML}{DBEAFE}
\definecolor{explAccent}{HTML}{2563EB}
\definecolor{implBG}{HTML}{FED7AA}
\definecolor{implAccent}{HTML}{EA580C}
\definecolor{txtAccent}{HTML}{059669}
\definecolor{imgAccent}{HTML}{EA580C}
\definecolor{unionBG}{HTML}{EDE9FE}
\definecolor{unionAccent}{HTML}{7C3AED}
\definecolor{hitRed}{HTML}{DC2626}
\definecolor{okGreen}{HTML}{10B981}
\definecolor{failGray}{HTML}{9CA3AF}

\begin{document}
\begin{tikzpicture}[
  font=\footnotesize,
  >={Stealth[length=2mm]},
  thin,
  chip/.style={rounded corners=2pt,inner sep=3pt,font=\scriptsize\bfseries,line width=0.6pt},
  panel/.style={rounded corners=4pt,draw=black!25,line width=0.5pt},
  probeLabel/.style={font=\scriptsize,anchor=west},
  verdictOK/.style={font=\scriptsize\bfseries,text=okGreen,anchor=west},
  verdictFail/.style={font=\scriptsize\bfseries,text=hitRed!75!black,anchor=west},
  colBG/.style={rounded corners=3pt,draw=black!15,line width=0.4pt},
]

% ============== layout constants ==============
\def\colW{6.6}
\pgfmathsetmacro{\Lx}{-\colW/2 - 0.5}  % -3.8
\pgfmathsetmacro{\Rx}{ \colW/2 + 0.5}  %  3.8

% ============== column backgrounds ==============
\node[colBG,fill=explBG!55,minimum width=\colW cm,minimum height=7.6cm,anchor=center]
     (bgL) at (\Lx, 1.0) {};
\node[colBG,fill=implBG!55,minimum width=\colW cm,minimum height=7.6cm,anchor=center]
     (bgR) at (\Rx, 1.0) {};

% ============== column headers ==============
\node[chip,fill=explBG,draw=explAccent,text=explAccent] at (\Lx, 4.3)
  {Explicit regime\ \textendash\ keyword-visible};
\node[chip,fill=implBG,draw=implAccent,text=implAccent] at (\Rx, 4.3)
  {Implicit regime\ \textendash\ keyword-evasive};

% ============== prompt bubbles ==============
\node[panel,fill=white,minimum width=6.0cm,minimum height=0.9cm,align=center,font=\scriptsize,anchor=center]
     (promptL) at (\Lx, 3.3)
     {\textit{``a \textcolor{hitRed}{\underline{\textbf{naked}}} body at the beach''}};
\node[panel,fill=white,minimum width=6.0cm,minimum height=0.9cm,align=center,font=\scriptsize,anchor=center]
     (promptR) at (\Rx, 3.3)
     {\textit{``a scene reminiscent of a battlefield at dusk''}};

% sub-label under prompts
\node[anchor=north,font=\tiny\itshape,text=explAccent!80!black] at (\Lx, 2.75)
     {keyword cue present};
\node[anchor=north,font=\tiny\itshape,text=implAccent!80!black] at (\Rx, 2.75)
     {no keyword\,-\,benign-looking words};

% ============== probe-response rows ==============
\def\yText{1.55}
\def\yImg{0.10}

% --- L : Text probe → strong (one column red) ---
\node[probeLabel] at (\Lx - 3.1, \yText) {\bfseries Text probe};
\foreach \r in {0,1,2,3}{
  \foreach \c in {0,1,2,3}{
    \pgfmathsetmacro{\tx}{\Lx - 0.5 + 0.32*\c}
    \pgfmathsetmacro{\ty}{\yText + 0.4 - 0.25*\r}
    \pgfmathtruncatemacro{\hit}{(\c==1)?1:0}
    \ifnum\hit=1
      \node[fill=hitRed!75,draw=hitRed,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
    \else
      \node[fill=txtAccent!15,draw=txtAccent!30,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
    \fi
  }
}
\node[verdictOK] at (\Lx + 1.3, \yText) {\checkmark\ fires};

% --- L : Image probe → moderate (3 cells orange) ---
\node[probeLabel] at (\Lx - 3.1, \yImg) {\bfseries Image probe};
\foreach \r in {0,1,2,3}{
  \foreach \c in {0,1,2,3}{
    \pgfmathsetmacro{\tx}{\Lx - 0.5 + 0.32*\c}
    \pgfmathsetmacro{\ty}{\yImg + 0.4 - 0.25*\r}
    \node[fill=imgAccent!15,draw=imgAccent!30,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
  }
}
\foreach \r/\c in {1/1, 2/1, 2/2}{
  \pgfmathsetmacro{\tx}{\Lx - 0.5 + 0.32*\c}
  \pgfmathsetmacro{\ty}{\yImg + 0.4 - 0.25*\r}
  \node[fill=imgAccent!75,draw=imgAccent,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
}
\node[verdictOK] at (\Lx + 1.3, \yImg) {\checkmark\ also fires};

% --- R : Text probe → EMPTY (fail) ---
\node[probeLabel] at (\Rx - 3.1, \yText) {\bfseries Text probe};
\foreach \r in {0,1,2,3}{
  \foreach \c in {0,1,2,3}{
    \pgfmathsetmacro{\tx}{\Rx - 0.5 + 0.32*\c}
    \pgfmathsetmacro{\ty}{\yText + 0.4 - 0.25*\r}
    \node[fill=failGray!12,draw=failGray!35,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
  }
}
\node[verdictFail] at (\Rx + 1.3, \yText) {$\boldsymbol{\times}$\ misses};

% --- R : Image probe → STRONG (scattered orange, more than left side) ---
\node[probeLabel] at (\Rx - 3.1, \yImg) {\bfseries Image probe};
\foreach \r in {0,1,2,3}{
  \foreach \c in {0,1,2,3}{
    \pgfmathsetmacro{\tx}{\Rx - 0.5 + 0.32*\c}
    \pgfmathsetmacro{\ty}{\yImg + 0.4 - 0.25*\r}
    \node[fill=imgAccent!15,draw=imgAccent!30,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
  }
}
\foreach \r/\c in {0/2, 1/1, 1/2, 2/0, 2/2, 3/1, 3/2}{
  \pgfmathsetmacro{\tx}{\Rx - 0.5 + 0.32*\c}
  \pgfmathsetmacro{\ty}{\yImg + 0.4 - 0.25*\r}
  \node[fill=imgAccent!80,draw=imgAccent,minimum size=2.5mm,inner sep=0pt] at (\tx,\ty) {};
}
\node[verdictOK] at (\Rx + 1.3, \yImg) {\checkmark\ catches it};

% ============== highlight callout on the implicit image-probe row ==============
% emphasize "text fails → image saves here" — the crux of the dual-probe story
\draw[hitRed!70,dashed,line width=0.7pt,rounded corners=3pt]
  (\Rx - 3.1, \yImg - 0.65) rectangle (\Rx + 3.1, \yImg + 0.65);
\node[font=\tiny\bfseries,text=hitRed!75!black,anchor=north east]
     at (\Rx + 3.1, \yImg + 0.62) {\ \ ours save};

% ============== concept chips beneath each side ==============
\node[font=\tiny,anchor=center,text=explAccent!80!black] at (\Lx, -1.2)
     {\textbf{concepts:} nudity, sexual};
\node[font=\tiny,anchor=center,text=implAccent!80!black] at (\Rx, -1.2)
     {\textbf{concepts:} violence, illegal, shocking, self-harm};

% ============== union fusion ==============
\coordinate (uTarget) at (0, -2.3);
\draw[->,draw=explAccent,line width=0.9pt] (\Lx + 1.6, -1.55) -- (uTarget);
\draw[->,draw=implAccent,line width=0.9pt] (\Rx - 1.6, -1.55) -- (uTarget);

\node[panel,fill=unionBG,draw=unionAccent,line width=0.8pt,
      minimum width=10cm,minimum height=1.3cm,align=center,font=\small,anchor=center]
     (union) at (0, -2.85)
     {$M \;=\; \min\bigl(1,\; M_{\mathrm{text}} + M_{\mathrm{img}}\bigr)$\\[2pt]
      {\scriptsize\itshape either probe fires $\ \Rightarrow\ $ the safety mask fires}};

% ============== tagline ==============
\node[align=center,font=\scriptsize\itshape,text=black!72,anchor=north]
     at (0, -3.75)
     {Keyword-only baselines (SAFREE, SLD, \dots) \textbf{fail on implicit prompts};\\
      the image probe catches what the text probe misses $\Rightarrow$ \emph{ambiguous adversarial cases are still detected}.};

% ============== deployment knob footnote ==============
\node[anchor=north,font=\tiny\itshape,text=black!55]
     at (0, -4.7) {\texttt{probe} $\in\{\text{text, image, both}\}$ is a per-deployment knob};

\end{tikzpicture}
\end{document}
